
# modellerde değişiklik yapınca:

python manage.py makemigrations
python manage.py migrate

# venv açmak için:
    # cmd
    venv\Scripts\activate

    # git bash
    source venv/Scripts/activate

# celery çalıştır:
celery -A airline worker -l info

local
python manage.py runserver